// This file previously contained nested DTO classes named TransactionDto, TransactionFilterDto,
// and TransactionSummaryFilterDto which conflicted with the project's DTO types in TatumConnectBackened.DTOs.
// Remove duplicate local DTOs to avoid type ambiguity. Use the DTOs in TatumConnectBackened.DTOs instead.

namespace TatumConnectBackened.Common.Constants
{
    // Intentionally left empty. Keep common transaction-related constants in TransactionStatus.cs
}
