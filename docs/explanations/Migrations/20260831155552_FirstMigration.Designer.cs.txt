Migrations/20260831155552_FirstMigration.Designer.cs — Purpose and key responsibilities

Purpose:
- Designer file generated by EF Core containing metadata about the migration for tooling (model snapshot, migration id).

What it contains:
- MigrationId, BuildTargetModel method which describes EF Core model metadata used by tools to compute future migrations.

Usage:
- Used by EF Core tooling; not typically edited by developers.

Notes:
- Keep this file with the migration to ensure consistent migrations across environments.